// Check that a username doesn't already exist
// if it's valid, accept the username
// if not, reject but keep the username filled
// rethink the param on this one
const updateUsername = async (body: any) => {};

export default updateUsername;
